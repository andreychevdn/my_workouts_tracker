import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  wrap: {
    position: "fixed",
    top: 0,
    left: "41%",
    zIndex: "1300",
  },
}));

export default useStyles;
